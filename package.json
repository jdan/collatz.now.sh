{
  "dependencies": {},
  "scripts": {
    "//": "In dev you'll probably want to run `watch` and `now dev` in two windows",
    "watch": "bsb -make-world -w",
    "clean": "bsb -clean-world && rm -rf api",
    "deploy": "bsb -make-world && now"
  },
  "devDependencies": {
    "bs-platform": "^7.0.1"
  }
}
