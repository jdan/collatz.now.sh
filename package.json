{
  "dependencies": {},
  "scripts": {
    "//": "In dev you'll probably want to run `watch` and `now dev` in two windows",
    "watch": "bsb -make-world -w",
    "clean": "bsb -clean-world && rm -rf api",
    "build-now": "bsb -make-world && mkdir -p api && cp src/*.js api",
    "deploy": "yarn build-now && now"
  },
  "devDependencies": {
    "bs-platform": "^7.0.1",
    "nodemon": "^2.0.2"
  }
}
